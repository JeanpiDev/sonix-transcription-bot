## Execute the next commands to setup

In the base project you will find the python version required 

```
# Install environment
python -m venv .venv 

# Linux 

source .venv/bin/activate

# Installation 

pip install -r requirements.txt

```